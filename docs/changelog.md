# Changelog

## 2026-08-14

- Добавлен исходник будущей статьи БЗ о разборе `method_operating` и оптимизации вызовов Bitrix24 API.

## 2026-07-09

- `telegram_retry_decorator` теперь повторяет transient `NetworkError`, но не ретраит логические `BadRequest`.
